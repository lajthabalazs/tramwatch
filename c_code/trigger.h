#ifndef TRIGGER_H#define TRIGGER_H#include "pnghelper.h" // crop, bitmap, pixel structuretypedef struct sttrch { int frame; int x; int y; int c; struct sttrch *next;} triggerchain_t;typedef struct { int *cells; int sizex; int sizey;} diff_t;#define HISTSIZE 15#define GRIDSIZE 12bitmap_t framebuffer[HISTSIZE];diff_t diffbuffer[HISTSIZE - 1];static int framebp = -1;void fdbuffer_init();void fdbuffer_destroy();int store_frame(bitmap_t *bitmap, crop_t *crop);void write_frame(int fn, char *filename);int calculate_alldiffs(int fref);int sumtrigger(triggerchain_t *tc);#endif